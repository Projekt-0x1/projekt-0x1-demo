# Runtime Requirements (Fragile)

This binary is **hardware/driver dependent** and may not run on all systems.

## Known requirements
- NVIDIA GPU with CUDA support
- Compatible NVIDIA driver
- CUDA runtime libraries (version matching build environment)
- Linux x86_64

If the binary fails to run, use the provided artifacts and `verify.sh` instead.
A working build can be provided on request for a specific environment.
